import { Routes } from '@angular/router';
import { StartPageComponent } from './presentation/routes/start-page.component/start-page.component';

export const routes: Routes = [{ path: '', component: StartPageComponent }];
